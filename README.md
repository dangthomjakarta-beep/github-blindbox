# GitHub 每日盲盒

每天 18:00，AI 自动从 GitHub Trending 筛选**真正值得关注的项目**，按「创意好玩 / 工具 / AI 应用」三类整理好，发到你 QQ 邮箱。

**服务器我已经搭好了，数据源我包了。** 你不需要任何服务器，Fork 我的仓库，加2个 Secret，就能收邮件。

## 数据来源：多语言覆盖，去重聚合

绝大多数 Trending 工具只看单一语言榜，视野狭窄。我们每天用 **5 个语言维度并行抓取**：

| 维度 | 用途 |
|------|------|
| `(全语言)` | 覆盖今日整体趋势 |
| `python` | 数据科学 / AI 生态 |
| `javascript` | 前端 / Node.js 生态 |
| `typescript` | 现代全栈生态 |
| `go` | 云原生 / 基础设施生态 |

5 路数据去重合并后，得到 **50-80 个候选项目**，再交由 AI 做第二轮筛选。这样既不会漏掉冷门语言的好项目，也不会因为单一维度的噪声错过真正值得关注的东西。

---

### 它和别的 Trending 产品有什么不同？

其他 GitHub Trending 相关产品（RSS 订阅、邮件简报、排行榜网站）做的事本质上就是一句话：**把热门 repo 列出来给你看**，排序方式无非是按星数或今日新增。你是那个被动接收信息的人，筛选全靠自己。

**我们做了三层过滤，不是简单的"挑几个好的"：**

**① 多源去重聚合 → 建立候选池**
通过 5 个语言维度（`python`、`javascript`、`typescript`、`go` 及全语言）分别抓取 Trending，拿到 50-80 个不重复项目。这一步解决了单语言 Trending 视野狭窄的问题。

**② 历史去重 → 避免信息疲劳**
自动排除前几天已经推送过的项目，保证每天都是新鲜内容，不会重复看到同一个项目。

**③ 选题引擎 + 人格化 AI 撰写 → 按"人"过滤，不是按"指标"**
这是最核心的一步。程序先过滤明显无关项目，再按新鲜度、主题、组织和 AI 项目配额组建候选池，最后让 AI 按一个**具体的读者画像**撰写：

> 企业 AI 落地、零售运营提效、知识库和自动化流程建设者，优先关注能迁移到真实业务的工具、产品思路和可复用方法。

选题引擎会限制同一主题和同一组织连续出现，并让一封邮件至少覆盖 4 个主题、AI 核心项目最多 2 个；AI 只负责对已经确定的项目写内容，不能临时换项目。

> ### 🧑‍🎨 这里的筛选画像，你可以自己改
>
> **这就是整个项目最核心的定制点。** Fork 后打开 `config/preferences.json`，修改 `readerProfile` 就能调整推荐方向；`diversityPolicy` 控制多样性，`hardFilters` 控制明显无关项目的过滤。
>
> 想扩大筛选范围就写松一点，想精准命中就写细一点。**这是你的项目，怎么筛你说了算。**

---



**全程浏览器操作，不需要终端。**

**你需要自己准备：**

| 需要准备 | 费用 | 时间 |
|---------|------|------|
| DeepSeek API Key | 1 元起充 | 3 分钟 |
| QQ 邮箱授权码 | 免费 | 2 分钟 |

详细步骤请看GUIDE.md

---

## 原理

```
我的服务器 05:00 抓数据
    ↓
push 到 GitHub（公开的数据源）
    ↓
你的 Fork → Actions 拉我的数据 → AI 筛选 → 发邮件到 QQ 邮箱
```

- 抓取是我干的，你 Fork 后只用配 DeepSeek + 邮箱
- 全部跑在 GitHub Actions 上，零成本
